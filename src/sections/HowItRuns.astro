---
---

<section id="how-it-runs" class="py-24 sm:py-32">
  <div class="max-w-5xl mx-auto px-6">
    <!-- Header -->
    <div class="text-center mb-12" data-reveal>
      <p class="text-emerald text-xs font-mono uppercase tracking-widest mb-3">Agents & Environments</p>
      <h2 class="font-heading font-bold text-3xl sm:text-4xl mb-4">
        One framework. Any agent. Three integration layers.
      </h2>
      <p class="text-text-secondary max-w-2xl mx-auto">
        SLOPE is a framework your AI agent follows — not a plugin.
        Set it up once, and every sprint produces structured data.
      </p>
    </div>

    <!-- Integration Layers -->
    <div class="grid md:grid-cols-3 gap-4 mb-10" data-stagger>
      <!-- CLI Tools -->
      <div class="rounded-xl border border-border bg-bg-card p-6">
        <div class="flex items-center gap-3 mb-3">
          <div class="w-8 h-8 rounded-lg bg-emerald/10 flex items-center justify-center shrink-0">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" class="text-emerald" aria-hidden="true">
              <polyline points="4 17 10 11 4 5" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" />
              <line x1="12" y1="19" x2="20" y2="19" stroke="currentColor" stroke-width="2" stroke-linecap="round" />
            </svg>
          </div>
          <h3 class="font-heading font-bold text-base text-text-primary">CLI Tools</h3>
        </div>
        <p class="text-text-muted text-sm leading-relaxed mb-3">
          The agent runs slope commands — briefing, review, card.
          Works in any environment that can execute shell commands.
        </p>
        <span class="text-[10px] font-mono px-2 py-1 rounded-full bg-emerald/10 text-emerald/80">All environments</span>
      </div>

      <!-- Rules Files -->
      <div class="rounded-xl border border-border bg-bg-card p-6">
        <div class="flex items-center gap-3 mb-3">
          <div class="w-8 h-8 rounded-lg bg-emerald/10 flex items-center justify-center shrink-0">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" class="text-emerald" aria-hidden="true">
              <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" />
              <polyline points="14 2 14 8 20 8" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" />
              <line x1="16" y1="13" x2="8" y2="13" stroke="currentColor" stroke-width="2" stroke-linecap="round" />
              <line x1="16" y1="17" x2="8" y2="17" stroke="currentColor" stroke-width="2" stroke-linecap="round" />
            </svg>
          </div>
          <h3 class="font-heading font-bold text-base text-text-primary">Rules Files</h3>
        </div>
        <p class="text-text-muted text-sm leading-relaxed mb-3">
          CLAUDE.md, .cursorrules, or AGENTS.md — the agent reads
          framework instructions at session start. Routines, checklists,
          commit discipline.
        </p>
        <span class="text-[10px] font-mono px-2 py-1 rounded-full bg-emerald/10 text-emerald/80">Claude Code · Cursor · OpenCode</span>
      </div>

      <!-- Hooks & Guards -->
      <div class="rounded-xl border border-border bg-bg-card p-6">
        <div class="flex items-center gap-3 mb-3">
          <div class="w-8 h-8 rounded-lg bg-emerald/10 flex items-center justify-center shrink-0">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" class="text-emerald" aria-hidden="true">
              <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" />
              <polyline points="9 12 11 14 15 10" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" />
            </svg>
          </div>
          <h3 class="font-heading font-bold text-base text-text-primary">Hooks & Guards</h3>
        </div>
        <p class="text-text-muted text-sm leading-relaxed mb-3">
          Guards fire automatically on tool-use events via Claude Code hooks.
          Real-time warnings before mistakes land — no agent action needed.
        </p>
        <span class="text-[10px] font-mono px-2 py-1 rounded-full bg-emerald/10 text-emerald/80">Claude Code</span>
      </div>
    </div>

    <!-- Agent Model -->
    <div class="grid sm:grid-cols-2 gap-4" data-stagger>
      <!-- Solo Agent -->
      <div class="rounded-xl border border-border bg-bg-surface p-5">
        <div class="flex items-center gap-2 mb-2">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" class="text-emerald shrink-0" aria-hidden="true">
            <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" />
            <circle cx="12" cy="7" r="4" stroke="currentColor" stroke-width="2" />
          </svg>
          <h3 class="font-heading font-bold text-base text-text-primary">Solo Agent</h3>
        </div>
        <p class="text-text-muted text-sm leading-relaxed">
          One agent, one sprint. Scorecards, handicap, and miss patterns
          track performance across sessions.
        </p>
      </div>

      <!-- Multi-Agent Team -->
      <div class="rounded-xl border border-border bg-bg-surface p-5">
        <div class="flex items-center gap-2 mb-2">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" class="text-emerald shrink-0" aria-hidden="true">
            <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" />
            <circle cx="9" cy="7" r="4" stroke="currentColor" stroke-width="2" />
            <path d="M23 21v-2a4 4 0 0 0-3-3.87" stroke="currentColor" stroke-width="2" stroke-linecap="round" />
            <path d="M16 3.13a4 4 0 0 1 0 7.75" stroke="currentColor" stroke-width="2" stroke-linecap="round" />
          </svg>
          <h3 class="font-heading font-bold text-base text-text-primary">Multi-Agent Team</h3>
        </div>
        <p class="text-text-muted text-sm leading-relaxed mb-2">
          Multiple agents, coordinated sprints. Built-in roles, ticket
          claiming, per-agent stats, team handicap.
        </p>
        <a href="/docs#team-multi-agent" class="text-emerald text-sm font-medium hover:underline inline-flex items-center gap-1">
          Learn more in the docs
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" aria-hidden="true">
            <path d="M5 12h14M12 5l7 7-7 7" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" />
          </svg>
        </a>
      </div>
    </div>
  </div>
</section>
