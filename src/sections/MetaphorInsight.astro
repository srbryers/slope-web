---
---

<section id="metaphor-insight" class="py-24 sm:py-32">
  <div class="max-w-4xl mx-auto px-6">
    <!-- Lead-in — flows from Problem's chaos -->
    <div class="text-center mb-16" data-reveal>
      <p class="text-emerald text-xs font-mono uppercase tracking-widest mb-3">How SLOPE Works</p>
      <h2 class="font-heading font-bold text-3xl sm:text-4xl mb-4">
        Structure. Methodology. Language.
      </h2>
      <p class="text-text-secondary max-w-2xl mx-auto">
        SLOPE layers a framework on top of agent work so the output is useful to humans — not just "done" or "not done", but structured data you can learn from.
      </p>
    </div>

    <!-- Layer 1: Structure -->
    <div class="mb-6" data-reveal>
      <div class="rounded-xl border border-border bg-bg-surface p-6 sm:p-8">
        <div class="flex items-center gap-3 mb-3">
          <span class="flex items-center justify-center w-8 h-8 rounded-full bg-emerald/10 text-emerald font-heading font-bold text-sm shrink-0">1</span>
          <h3 class="text-text-primary font-heading font-bold text-lg">Structure</h3>
        </div>
        <p class="text-text-muted leading-relaxed">
          SLOPE organizes agent work into sprints — focused batches with a clear scope, a beginning, and an end. Instead of an endless stream of sessions, each batch becomes a unit you can measure.
        </p>
      </div>
    </div>

    <!-- Layer 2: Methodology -->
    <div class="mb-6" data-reveal>
      <div class="rounded-xl border border-border bg-bg-surface p-6 sm:p-8">
        <div class="flex items-center gap-3 mb-3">
          <span class="flex items-center justify-center w-8 h-8 rounded-full bg-emerald/10 text-emerald font-heading font-bold text-sm shrink-0">2</span>
          <h3 class="text-text-primary font-heading font-bold text-lg">Methodology</h3>
        </div>
        <p class="text-text-muted leading-relaxed">
          Each sprint follows a repeatable routine: set expectations, declare difficulty, execute, score the result, review what happened. This turns raw work into structured data — how hard it was, what went wrong, and whether patterns are emerging.
        </p>
      </div>
    </div>

    <!-- Layer 3: Language -->
    <div class="mb-12" data-reveal>
      <div class="rounded-xl border border-emerald/20 bg-bg-surface p-6 sm:p-8">
        <div class="flex items-center gap-3 mb-3">
          <span class="flex items-center justify-center w-8 h-8 rounded-full bg-emerald/10 text-emerald font-heading font-bold text-sm shrink-0">3</span>
          <h3 class="text-emerald font-heading font-bold text-lg">Language</h3>
        </div>
        <p class="text-text-muted leading-relaxed">
          Then SLOPE wraps that data in a metaphor your team already understands. Golf, gaming, D&D — the metaphor makes the data intuitive. Same engine, different vocabulary.
        </p>
      </div>
    </div>

    <!-- The twist — sets up SeeItInAction -->
    <div class="mb-12" data-reveal>
      <p class="text-center text-text-secondary text-xl sm:text-2xl font-heading leading-relaxed max-w-2xl mx-auto">
        The scoring engine doesn't change. Only the language does.
      </p>
    </div>

    <!-- Mapping visual: raw work → SLOPE layer → what you gain -->
    <div class="grid grid-cols-3 gap-3 sm:gap-6 max-w-2xl mx-auto" data-stagger>
      <div class="text-center">
        <div class="rounded-lg border border-border bg-bg-card p-3 sm:p-4">
          <p class="text-text-muted text-[10px] sm:text-xs font-mono uppercase tracking-wider mb-1">Raw Work</p>
          <p class="text-text-primary text-sm sm:text-base font-heading font-bold">Sessions</p>
        </div>
        <p class="text-text-muted text-[10px] sm:text-xs mt-2">What agents produce</p>
      </div>
      <div class="text-center">
        <div class="rounded-lg border border-emerald/20 bg-emerald/5 p-3 sm:p-4">
          <p class="text-emerald text-[10px] sm:text-xs font-mono uppercase tracking-wider mb-1">SLOPE</p>
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" class="mx-auto mb-1 text-emerald/60" aria-hidden="true">
            <path d="M5 12h14M12 5l7 7-7 7" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" />
          </svg>
          <p class="text-emerald text-sm sm:text-base font-heading font-bold">Hole · Level · Encounter</p>
        </div>
        <p class="text-text-muted text-[10px] sm:text-xs mt-2">How it gets framed</p>
      </div>
      <div class="text-center">
        <div class="rounded-lg border border-border bg-bg-card p-3 sm:p-4">
          <p class="text-text-muted text-[10px] sm:text-xs font-mono uppercase tracking-wider mb-1">You Gain</p>
          <p class="text-text-primary text-sm sm:text-base font-heading font-bold">Insight</p>
        </div>
        <p class="text-text-muted text-[10px] sm:text-xs mt-2">Data you can act on</p>
      </div>
    </div>
  </div>
</section>
